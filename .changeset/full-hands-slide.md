---
"pd-markdown-ui": patch
---

Add integration examples in `apps/web` demonstrating `pd-markdown-ui` with `react-markdown` and `remark-gfm`.
Includes a new `MarkdownExample` component showcasing typography, lists, code blocks, and tables.
