---
"pd-shad-ui": major
---

Fix prefixed Tailwind state utilities in `pd-shad-ui`, restore valid `use client` directives for client-only components, publish a documented `styles.css` entry for package consumers, and add Vue form primitives for closer React/Vue API parity.
